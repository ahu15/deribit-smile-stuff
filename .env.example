# Copy to .env and fill in your read-only Deribit API key (optional —
# anonymous REST calls work too, just with smaller rate-limit headroom).
DERIBIT_CLIENT_ID=
DERIBIT_CLIENT_SECRET=
# Set to "true" to use test.deribit.com
DERIBIT_TESTNET=false
